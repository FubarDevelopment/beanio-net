using System;

namespace BeanIO.Builder
{
    public enum Align
    {
        /// <summary>
        /// Text is aligned to the left
        /// </summary>
        Left,

        /// <summary>
        /// Text is aligned to the right
        /// </summary>
        Right,
    }
}
